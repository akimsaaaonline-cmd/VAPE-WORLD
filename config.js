/**
 * VAPE WORLD — shop API address.
 *
 * Empty string = the API lives on the same address as this page (self-hosted
 * copy, or the desktop app's built-in server). A full URL = talk to the online
 * shop, which is what static hosts such as GitHub Pages and Vercel need.
 */
window.__VW_API__ = "https://vapeworlds1.pplx.app/port/9000";
